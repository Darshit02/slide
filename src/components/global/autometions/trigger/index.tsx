import React from 'react'

type Props = {
    id: string
  }

  const Trigger = ({ id }: Props) => {
  return (
    <div className="flex flex-col ga-y-6 items-center">index</div>
  )
}

export default Trigger